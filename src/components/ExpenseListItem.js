import React from 'react';
import { connect} from 'react-redux';

// export stateless functional component
//desc, amount, createddAt

const ExpenseListItem = (
    <div>
        <h1>Expense Item</h1>
    </div>
);

export default ConnectedExpenseItem = connect()(ExpenseListItem);